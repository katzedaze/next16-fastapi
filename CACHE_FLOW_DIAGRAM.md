# Next.js 16 Cache Flow Visualization

## 🔄 `use cache` フロー

```
┌─────────────────────────────────────────────────────────────┐
│                    User Action Flow                          │
└─────────────────────────────────────────────────────────────┘

Step 1: Initial Page Load
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ GET /items-use-cache
       ▼
┌──────────────────────┐
│   Next.js Server     │
│                      │
│  async getItems() {  │
│    'use cache'       │◄─── Cache MISS (first time)
│    cacheTag('items') │
│    cacheLife('mins') │
│  }                   │
└──────┬───────────────┘
       │ fetch('http://localhost:8000/api/items')
       ▼
┌──────────────────────┐
│   FastAPI Backend    │
│                      │
│  GET /api/items      │
│  → Query Database    │
│  → Return JSON       │
└──────┬───────────────┘
       │ Response: {"items": [...], "total": 1}
       ▼
┌──────────────────────┐
│   Next.js Server     │
│                      │
│  Cache the result    │◄─── Cache STORED
│  with tag 'items'    │     Expires in 1 minute
│  and lifetime 1min   │
└──────┬───────────────┘
       │ HTML Response
       ▼
┌──────────────────────┐
│   Browser            │
│                      │
│  Render page         │
│  Show timestamp      │
│  Show items list     │
└──────────────────────┘

Step 2: User Creates New Item
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ Click "Create New Item"
       ▼
┌──────────────────────┐
│  /items-create page  │
│                      │
│  Fill form           │
│  Submit              │
└──────┬───────────────┘
       │ POST /api/items
       ▼
┌──────────────────────┐
│   FastAPI Backend    │
│                      │
│  POST /api/items     │
│  → Insert to DB      │
│  → Return new item   │
└──────┬───────────────┘
       │ Response: {"id": 2, ...}
       ▼
┌──────────────────────┐
│   Browser            │
│                      │
│  Success message     │
│  "Item created!"     │
└──────────────────────┘

Step 3: Return to List Page (Within 1 minute)
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ Navigate back to /items-use-cache
       ▼
┌──────────────────────┐
│   Next.js Server     │
│                      │
│  async getItems() {  │
│    'use cache'       │◄─── Cache HIT!
│  }                   │     No API call
└──────┬───────────────┘
       │ Return cached data (old data, 1 item)
       ▼
┌──────────────────────┐
│   Browser            │
│                      │
│  ⚠️ OLD timestamp    │
│  ⚠️ OLD items (1)    │
│  ℹ️  New item NOT    │
│     shown yet        │
└──────────────────────┘

Step 4: After 1 Minute - Reload Page
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ Reload page (after 1+ minute)
       ▼
┌──────────────────────┐
│   Next.js Server     │
│                      │
│  async getItems() {  │
│    'use cache'       │◄─── Cache EXPIRED
│  }                   │     Re-fetch from API
└──────┬───────────────┘
       │ fetch('http://localhost:8000/api/items')
       ▼
┌──────────────────────┐
│   FastAPI Backend    │
│                      │
│  GET /api/items      │
│  → Query Database    │
│  → Return JSON       │
└──────┬───────────────┘
       │ Response: {"items": [...], "total": 2}
       ▼
┌──────────────────────┐
│   Next.js Server     │
│                      │
│  Cache new result    │◄─── Cache UPDATED
│  with tag 'items'    │     Expires in 1 minute
└──────┬───────────────┘
       │ HTML Response
       ▼
┌──────────────────────┐
│   Browser            │
│                      │
│  ✅ NEW timestamp    │
│  ✅ NEW items (2)    │
│  ✅ Created item     │
│     now visible      │
└──────────────────────┘
```

## 🌊 Suspense フロー

```
┌─────────────────────────────────────────────────────────────┐
│                  Suspense Action Flow                        │
└─────────────────────────────────────────────────────────────┘

Step 1: Initial Page Load
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ GET /items-suspense
       ▼
┌──────────────────────────────────┐
│   Next.js Server                 │
│                                  │
│  <Suspense fallback={Loading}>   │
│    <ItemsList />                 │
│  </Suspense>                     │
└──────┬───────────────────────────┘
       │ 1. Send HTML shell immediately
       ▼
┌──────────────────────────────────┐
│   Browser                        │
│                                  │
│  Show Loading skeleton...        │◄─── Streaming SSR!
└──────────────────────────────────┘
       │
       │ 2. Meanwhile, fetch data
       ▼
┌──────────────────────────────────┐
│   Next.js Server                 │
│                                  │
│  async ItemsList() {             │
│    const data = await fetch()    │◄─── Default: 'force-cache'
│  }                               │
└──────┬───────────────────────────┘
       │ fetch('http://localhost:8000/api/items')
       ▼
┌──────────────────────────────────┐
│   FastAPI Backend                │
│                                  │
│  GET /api/items                  │
│  → Query Database                │
└──────┬───────────────────────────┘
       │ Response: {"items": [...], "total": 1}
       ▼
┌──────────────────────────────────┐
│   Next.js Server                 │
│                                  │
│  Cache at BUILD time             │◄─── Cached indefinitely!
│  (Static Generation)             │
└──────┬───────────────────────────┘
       │ 3. Stream the component
       ▼
┌──────────────────────────────────┐
│   Browser                        │
│                                  │
│  Replace Loading with Items      │
│  Show items list                 │
└──────────────────────────────────┘

Step 2: User Creates New Item
(Same as use cache flow)

Step 3: Return to List Page (Anytime)
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ Navigate back to /items-suspense
       ▼
┌──────────────────────────────────┐
│   Next.js Server                 │
│                                  │
│  async ItemsList() {             │
│    const data = await fetch()    │◄─── Cache HIT!
│  }                               │     (Build-time cache)
└──────┬───────────────────────────┘
       │ Return cached data (old data)
       │ NO API call
       ▼
┌──────────────────────────────────┐
│   Browser                        │
│                                  │
│  ⚠️ Page timestamp may change    │
│  ⚠️ OLD items (1)                │
│  ℹ️  New item NOT shown          │
│  ℹ️  Need rebuild to see it      │
└──────────────────────────────────┘

Step 4: Developer Rebuilds (Production)
┌──────────────────────────────────┐
│   Developer Terminal             │
└──────┬───────────────────────────┘
       │ npm run build
       ▼
┌──────────────────────────────────┐
│   Next.js Build Process          │
│                                  │
│  Re-fetch all data               │
│  Generate static pages           │
└──────┬───────────────────────────┘
       │ fetch('http://localhost:8000/api/items')
       ▼
┌──────────────────────────────────┐
│   FastAPI Backend                │
│                                  │
│  GET /api/items                  │
└──────┬───────────────────────────┘
       │ Response: {"items": [...], "total": 2}
       ▼
┌──────────────────────────────────┐
│   Next.js Build                  │
│                                  │
│  ✅ Static pages updated         │
│  ✅ New item now in cache        │
└──────────────────────────────────┘
```

## 📊 Cache Timeline Comparison

```
Time: 0s (Initial load)
────────────────────────────────────────────────────────────
use cache:    [Fetch] → [Cache: 60s] → Render (1 item)
Suspense:     [Fetch] → [Cache: ∞]  → Render (1 item)
────────────────────────────────────────────────────────────

Time: 30s (User creates item #2)
────────────────────────────────────────────────────────────
Backend DB:   [Item #1] [Item #2] ✅
────────────────────────────────────────────────────────────

Time: 35s (Return to list page)
────────────────────────────────────────────────────────────
use cache:    [Cache HIT] → Render (1 item) ⚠️ Old!
              Cache expires in 25s...
Suspense:     [Cache HIT] → Render (1 item) ⚠️ Old!
              Never expires automatically
────────────────────────────────────────────────────────────

Time: 65s (Reload page - past 1 minute)
────────────────────────────────────────────────────────────
use cache:    [Cache MISS] → [Fetch] → Render (2 items) ✅
              New cache: 60s
Suspense:     [Cache HIT] → Render (1 item) ⚠️ Still old!
              Rebuild required
────────────────────────────────────────────────────────────

After Rebuild (Developer action)
────────────────────────────────────────────────────────────
Suspense:     [Build Fetch] → Render (2 items) ✅
              New static cache
────────────────────────────────────────────────────────────
```

## 🎯 Key Differences Visualized

### `use cache` - Time-based Revalidation

```
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│  Fetch  │→ │ Cache   │→ │ Expired │→ │ Re-fetch│→ (repeat)
│  Data   │  │ 1 min   │  │ @ 60s   │  │  Data   │
└─────────┘  └─────────┘  └─────────┘  └─────────┘
     ↓            ↓            ↓            ↓
   Fresh      Cached       Stale        Fresh
```

### Suspense - Build-time Static

```
┌─────────┐  ┌─────────────────────────────────┐
│  Build  │→ │      Cached Indefinitely        │
│  Fetch  │  │  (Until next build/revalidate)  │
└─────────┘  └─────────────────────────────────┘
     ↓                      ↓
   Fresh              Always "Fresh"
                    (from build time)
```

## 🔧 Manual Cache Invalidation

### use cache with revalidateTag()

```typescript
// In Server Action or Route Handler
import { revalidateTag } from 'next/cache';

export async function createItemAction(formData: FormData) {
  'use server';

  // Create item
  await createItem({...});

  // Immediately invalidate cache
  revalidateTag('items');  // ← Tagged in getItems()

  return { success: true };
}
```

**Flow:**

```
Create Item → revalidateTag('items') → Cache Cleared
                                      ↓
                            Next request fetches fresh data
```

### Suspense with revalidatePath()

```typescript
// In Server Action
import { revalidatePath } from 'next/cache';

export async function createItemAction(formData: FormData) {
  'use server';

  await createItem({...});

  // Invalidate entire path
  revalidatePath('/items-suspense');

  return { success: true };
}
```

**Flow:**

```
Create Item → revalidatePath() → Route Cache Cleared
                                 ↓
                     Next request re-renders from scratch
```

## 📈 Performance Characteristics

### use cache

```
Initial Load:    ████████░░ (Fetch from API)
Cached Load:     ██░░░░░░░░ (Instant - from cache)
After Expire:    ████████░░ (Re-fetch from API)
Update Latency:  ⏱️ Max 1 minute (configurable)
```

### Suspense

```
Initial Load:    ██████░░░░ (Streaming - partial fast)
Cached Load:     █░░░░░░░░░ (Instant - static)
After Update:    █░░░░░░░░░ (Still instant - old data)
Update Latency:  ⏱️ Until rebuild (manual action)
```

## 🎓 Which to Use When?

### Use `use cache` when

- ✅ Data changes frequently (every few minutes/hours)
- ✅ You need predictable revalidation
- ✅ You're using Server Actions to modify data
- ✅ You want fine-grained cache control

### Use Suspense when

- ✅ Data is mostly static (blog posts, product details)
- ✅ You want excellent loading UX
- ✅ You're okay with manual revalidation
- ✅ You want simplicity over control

---

**Visualization complete!** 🎨

Use these diagrams to understand how each caching strategy works and when to use them.
